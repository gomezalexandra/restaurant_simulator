{% extends 'base.html.twig' %}

{% block title %} dashboard {% endblock %}

{% block container %}

    <div class="row title">
        <h1 class="col-lg-12">
            Tableau de Bord
        </h1>
    </div>

    <p class="board"> Bonjour user !</p>

    <div class="row">
        <a class="col-sm-12" href="{{ path('app_homepage') }}"> Se déconnecter </a>
    </div>
    <div class="row">
        <p class="col-sm-12"> Dernière connexion: le 10/06/2020 à 10h </p>
    </div>

    {#<div class="row">
        <p class="offset-lg-1 col-lg-11">
            {% for user in users %}
                votre id : {{user.id}} </br>
                votre pseudo : {{user.pseudo}} </br>
                votre role : {{user.role}}
            {% endfor %}
        </p>
    </div>#}

    <div class="board">
        <div class="row d-flex justify-content-center">
            <h2 class="dashboardBlock blockStyle1 d-flex align-items-center justify-content-center col-sm-8"><a href="{{ path('app_new_simulation') }}"> Nouvelle simulation </a></h2>
        </div>

        <div class="row d-flex justify-content-center">
            <h2 class="dashboardBlock blockStyle2 d-flex align-items-center justify-content-center col-sm-4"><a href="{{ path('app_simulation_saved') }}">Les simulations</a></h2>
            <img class="dashboardBlock col-sm-4 dashboardFlower" src="{{ asset('build/images/flower.jpg') }}">
        </div>
        <div class="row d-flex justify-content-center">
            <h2 class="dashboardBlock blockStyle3 d-flex align-items-center justify-content-center col-sm-4"><a href="{{ path('app_simulation_draft') }}">Les brouillons</a></h2>
            <h2 class="dashboardBlock blockStyle4 d-flex align-items-center justify-content-center col-sm-4"><a href="{{ path('app_profile') }}">Mon profil</a></h2>
        </div>
    </div>

    {#<p> <a href="{{ path('app_dashboard', {route: 'new_simulation'}) }}">Nouvelle simulation</a> </p>
    <p> <a href="{{ path('app_dashboard', {route: 'simulation_draft'}) }}">Voir les brouillons</a> </p>
    <p> <a href="{{ path('app_dashboard', {route: 'simulation_saved'}) }}">Voir les simulations</a> </p>#}
{% endblock %}